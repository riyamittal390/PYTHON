# Type hints re added using the colon (:) syntax for variables and the -> syntax for function return types.

n : int = 5
name : str = "Riya"

def sum(a : int, b : int) -> int:
    return a + b 

sum(3) 